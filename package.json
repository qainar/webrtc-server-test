{
  "name": "webrtc-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index --https --cert certs/cert.pem --key certs/key.pem --port 8086 --host 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.12.1"
  }
}
